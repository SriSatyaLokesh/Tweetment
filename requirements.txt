sudo apt-get install python3-pip
sudo python3 -m pip install textblob
sudo python3 -m pip install tweepy
sudo python3 -m pip install SpeechRecognition 
sudo python3 -m pip install matplotlib
sudo apt-get install python3-tk
sudo python3 -m pip install numpy 
sudo apt install portaudio19-dev python-all-dev
sudo python3 -m pip install pyaudio==0.2.9 --upgrade
sudo python3 -m pip install flask
